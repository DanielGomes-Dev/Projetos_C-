using System;

namespace SoftBlueFirstProgram
{
    class Program
    {
        static void Main(string[] args)
        {
            double d = Math.Pow(10, 2);
            Console.WriteLine(d);
            //Snippets Duas vezes o tab
            //Edit -> intellisence -> snippets
            if (d > 0)
            {

            }
            else if (true) { }
            else { }

            //Ctrl+K , Ctrl+; -> Comenta em linha;
            //Ctrl+Shift+; -> comenta em bloco;
            //Ctrl+K, Ctrl+D; -> Formata Documento;
            for (int i = 0; i < 10; i++)
            {

            }
            //cw
            Console.WriteLine("asd");

            //Debug
        }
    }
}
